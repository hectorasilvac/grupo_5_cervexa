<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>

<body>
    <%- include('../partials/header') %>
    <main class="m1">
        <section class="register-form s1">
            <header>
                <h2>Crear una cuenta</h2>
                <p>Por favor diligencia la siguiente información para crear tu cuenta.</p>
            </header>
            <form action="/users/create" method="POST">
                <p class="form-entry">
                    <label for="firstName">Nombre</label>
                    <input id="firstName" type="text"
                        class="<%= locals.errors && errors.firstName ? 'is-invalid' : null %>" name="firstName"
                        value="<%= locals.oldData ? oldData.firstName : null %>" placeholder="Nombre" />
                    <% if (locals.errors && errors.firstName) {%>
                    <span><%= errors.firstName.msg %></span>
                    <% }%>
                </p>
                <p class="form-entry">
                    <label for="lastName">Apellido</label>
                    <input id="lastName" type="text"
                        class="<%= locals.errors && errors.lastName ? 'is-invalid' : null %>" name="lastName"
                        value="<%= locals.oldData ? oldData.lastName : null %>" placeholder="Apellido" />
                    <% if (locals.errors && errors.lastName) {%>
                    <span><%= errors.lastName.msg %></span>
                    <% }%>
                </p>
                <p class="form-entry">
                    <label for="password">Contraseña</label>
                    <input id="password" type="password"
                        class="<%= locals.errors && errors.password ? 'is-invalid' : null %>" name="password"
                        placeholder="Contraseña" />
                    <% if (locals.errors && errors.password) {%>
                    <span><%= errors.password.msg %></span>
                    <% }%>
                </p>
                <p class="form-entry">
                    <label for="email">Correo Electrónico</label>
                    <input id="email" type="email" class="<%= locals.errors && errors.email ? 'is-invalid' : null %>"
                        name="email" value="<%= locals.oldData ? oldData.email : null %>"
                        placeholder="Correo electrónico" />
                    <% if (locals.errors && errors.email) {%>
                    <span><%= errors.email.msg %></span>
                    <% }%>
                </p>
                <p class="form-entry p1">
                    <p class="l1">Acepto términos y condiciones</p>
                    <input type="radio" id="acceptedTerms" name="acceptedTerms" value="yes" checked/>
                    <label for="acceptedTerms">Sí</label>
                    <input type="radio" id="termsNotAccepted" name="acceptedTerms" value="no"/>
                    <label for="termsNotAccepted">No</label>
                    <% if (locals.errors && errors.acceptedTerms) {%>
                        <br/>
                    <span><%= errors.acceptedTerms.msg %></span>
                    <% }%>
                </p>
                <button class="b1" type="submit">Continuar</button>
            </form>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>

</html>